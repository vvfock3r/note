import type { App } from '@vuepress/core';
import type { UserConfig } from 'vite';
export declare const resolveDefine: ({ app, isBuild, isServer, }: {
    app: App;
    isBuild: boolean;
    isServer: boolean;
}) => Promise<UserConfig['define']>;
